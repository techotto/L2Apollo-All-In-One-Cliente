# BotApollo - regras (ordem = prioridade)
#
# FIXED: so clica quando o script emitir
#   C:\Users\Public\l2apollo.botapollo.fixed
# (PvP Event acabando / EXIT). Sem o arquivo, esta regra e IGNORADA.
images/fixed.png -> @self | 0.85

# Outras regras (sempre ativas)
images/rule5.png -> rule5-1.png | 0.85
images/imagem.png -> @self
